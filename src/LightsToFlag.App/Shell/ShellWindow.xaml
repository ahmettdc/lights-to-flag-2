<Window x:Class="LightsToFlag.App.Shell.ShellWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Lights to Flag 2" Height="760" Width="1200"
        MinHeight="620" MinWidth="960"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource TrackBlackBrush}"
        Foreground="{StaticResource WhiteBrush}"
        FontFamily="{StaticResource FontBody}" FontSize="14"
        TextOptions.TextFormattingMode="Ideal"
        WindowStyle="None" ResizeMode="CanResize">

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="42" ResizeBorderThickness="6"
                            GlassFrameThickness="0" CornerRadius="0" UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>

    <DockPanel>
        <!-- Custom title bar -->
        <Grid DockPanel.Dock="Top" Height="42" Background="{StaticResource TrackBlackBrush}">
            <Image Source="pack://application:,,,/Assets/Brand/logo-horizontal.png" Stretch="Uniform"
                   Height="20" HorizontalAlignment="Left" Margin="14,0,0,0" VerticalAlignment="Center" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="MinButton" Style="{StaticResource CaptionButton}" Content="&#xE921;" Click="OnMinimize" />
                <Button x:Name="MaxButton" Style="{StaticResource CaptionButton}" Content="&#xE922;" Click="OnMaximizeRestore" />
                <Button x:Name="CloseButton" Style="{StaticResource CloseCaptionButton}" Content="&#xE8BC;" Click="OnClose" />
            </StackPanel>
        </Grid>

        <!-- Active page -->
        <ContentControl Content="{Binding CurrentPage}" />
    </DockPanel>
</Window>
