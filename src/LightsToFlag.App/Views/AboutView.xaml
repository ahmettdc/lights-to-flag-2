<UserControl x:Class="LightsToFlag.App.Views.AboutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="{StaticResource TrackBlackBrush}">
        <StackPanel Width="520" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Image Source="pack://application:,,,/Assets/Brand/logo-primary.png"
                   Stretch="Uniform" MaxHeight="150" HorizontalAlignment="Left" Margin="0,0,0,24" />
            <TextBlock Text="{Binding Blurb}" TextWrapping="Wrap" FontSize="15" LineHeight="22" Margin="0,0,0,16" />
            <TextBlock Text="{Binding Credits}" Style="{StaticResource MutedText}" TextWrapping="Wrap" Margin="0,0,0,28" />
            <Button Content="BACK" Style="{StaticResource GhostButton}" Command="{Binding BackCommand}" HorizontalAlignment="Left" />
        </StackPanel>
    </Grid>
</UserControl>
