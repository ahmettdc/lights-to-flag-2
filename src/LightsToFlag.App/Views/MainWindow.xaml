<Window x:Class="LightsToFlag.App.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Lights to Flag" Height="600" Width="900"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Lights to Flag" FontSize="28" FontWeight="Bold" />
        <TextBlock Grid.Row="1" Margin="0,4,0,16" Foreground="Gray"
                   Text="F1 Management — rewrite (WPF shell placeholder, M0). Career UI arrives in M7–M9." />

        <!-- Placeholder: proves Core is wired in. Real carset picker + career flow comes in M7. -->
        <TextBlock Grid.Row="2" x:Name="StatusText" TextWrapping="Wrap" VerticalAlignment="Top" />
    </Grid>
</Window>
